<video
  class="h-full w-full object-cover absolute -z-10 left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 hue-rotate-15 opacity-0"
  autoplay
  loop
  muted
  playsinline
>
  <source src="/assets/videos/hero.mp4" type="video/mp4" />
</video>

<style>
  video {
    animation: fadeIn 500ms ease-in-out 500ms forwards;
  }

  @keyframes fadeIn {
    to {
      opacity: 1;
    }
  }
</style>
